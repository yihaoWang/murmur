---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [readme]

must_haves:
  truths:
    - "README.md exists at repo root with project overview, setup, and usage"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      min_lines: 40
  key_links: []
---

<objective>
Create README.md for the Typeness Swift project.

Purpose: Provide clear documentation for the open-source repo covering what the app does, how to build it, and how to use it.
Output: README.md at repo root
</objective>

<execution_context>
@/Users/yihao.wang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yihao.wang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Create README.md at repo root with these sections:

1. **Title and tagline** — "Typeness" with one-line description: native macOS voice-to-text input tool. Mention Traditional Chinese focus, local processing, Apple Silicon.

2. **Features** — Bullet list:
   - Global hotkey activation (toggle and push-to-talk modes)
   - Speech-to-text via whisper.cpp (large-v3-turbo model, CoreML/Accelerate)
   - Text formatting via local LLM (Qwen3-1.7B via MLX Swift)
   - Text insertion at cursor in any app (Accessibility API with clipboard fallback)
   - Menu bar app with SwiftUI settings
   - Debug mode with WAV + JSON session archiving
   - Auto-start at login
   - Confirm-before-insert mode
   - 100% local processing, no data sent externally

3. **Requirements** — macOS 14+ (Sonoma), Apple Silicon (M1+)

4. **Building** — `swift build` from repo root. Note: app requires Accessibility permissions and microphone access. Sandbox is disabled (required for CGEventTap global hotkeys).

5. **Architecture** — Brief list of key components from PROJECT.md (TypenessApp, HotkeyMonitor, AudioCaptureEngine, TranscriptionEngine, PostProcessingEngine, TextInsertionEngine, ModelManager, DebugArchiver, AppState, SettingsStore) with one-line descriptions.

6. **Tech Stack** — SwiftUI, AVAudioEngine, whisper.cpp (SwiftWhisper), MLX Swift (MLXLLM), CGEventTap, Accessibility API

7. **Privacy** — All speech recognition and text processing happens locally on device. No data is sent to external services.

8. **License** — Leave as "TBD" or omit if no LICENSE file exists.

Keep tone concise and technical. No emojis. Use standard markdown formatting.
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '{if ($1 >= 40) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>README.md exists at repo root with all sections, minimum 40 lines</done>
</task>

</tasks>

<verification>
README.md exists and covers project overview, features, requirements, building, architecture, and tech stack.
</verification>

<success_criteria>
- README.md present at repo root
- Contains accurate project information matching PROJECT.md
- Readable and well-structured
</success_criteria>

<output>
After completion, create `.planning/quick/260317-e52-write-readme-md/260317-e52-SUMMARY.md`
</output>
