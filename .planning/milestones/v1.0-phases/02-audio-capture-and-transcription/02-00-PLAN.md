---
phase: 02-audio-capture-and-transcription
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - Package.swift
  - Tests/TypenessTests/AudioCaptureTests.swift
  - Tests/TypenessTests/VADTests.swift
  - Tests/TypenessTests/WhisperBridgeTests.swift
autonomous: true
requirements: [AUDIO-01, AUDIO-03, STT-01, STT-03]

must_haves:
  truths:
    - "XCTest target TypenessTests exists and compiles"
    - "Test stubs exist for AudioCapture, VAD, and WhisperBridge"
  artifacts:
    - path: "Tests/TypenessTests/AudioCaptureTests.swift"
      provides: "XCTest stubs for audio capture format and permission"
      contains: "class AudioCaptureTests"
    - path: "Tests/TypenessTests/VADTests.swift"
      provides: "XCTest stubs for VAD silence rejection and minimum duration"
      contains: "class VADTests"
    - path: "Tests/TypenessTests/WhisperBridgeTests.swift"
      provides: "XCTest stub for transcription engine load/transcribe contract"
      contains: "class WhisperBridgeTests"
  key_links:
    - from: "Package.swift"
      to: "Tests/TypenessTests"
      via: "testTarget declaration with dependency on Typeness"
      pattern: "testTarget.*TypenessTests"
---

<objective>
Create the XCTest test target and stub test files so that plans 01 and 02 have automated test verification.

Purpose: Satisfies Nyquist compliance — every implementation task must have an automated test verify command.
Output: TypenessTests target in Package.swift, three stub test files.
</objective>

<execution_context>
@/Users/yihao.wang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yihao.wang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-capture-and-transcription/02-RESEARCH.md
@.planning/phases/02-audio-capture-and-transcription/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypenessTests target to Package.swift and create stub test files</name>
  <files>Package.swift, Tests/TypenessTests/AudioCaptureTests.swift, Tests/TypenessTests/VADTests.swift, Tests/TypenessTests/WhisperBridgeTests.swift</files>
  <read_first>
    - Package.swift
    - .planning/phases/02-audio-capture-and-transcription/02-VALIDATION.md
  </read_first>
  <action>
1. Update Package.swift — add a test target after the existing executableTarget:
   ```swift
   .testTarget(
       name: "TypenessTests",
       dependencies: ["Typeness"],
       path: "Tests/TypenessTests"
   )
   ```
   Note: The test target depends on "Typeness" so it can import the module. Since Typeness is an executableTarget, if import fails due to @main, use `@testable import` and ensure test files can reference the types. If the executable target cannot be imported in tests (common with @main apps), create the test stubs with placeholder assertions that will be filled in by plans 01 and 02 — the stubs should test the public API of AudioCaptureEngine, VADGate, and TranscriptionEngine by importing the types directly. If direct import is blocked by the executable target, add a comment explaining the limitation and use `XCTSkip("Requires library target refactor")` as a temporary measure.

2. Create `Tests/TypenessTests/AudioCaptureTests.swift`:
   ```swift
   import XCTest
   @testable import Typeness

   final class AudioCaptureTests: XCTestCase {
       func testTargetFormatIs16kHzMonoFloat32() throws {
           // STUB: Plan 02-01 will implement after AudioCaptureEngine exists
           // Verify: AudioCaptureEngine.targetFormat.sampleRate == 16_000
           // Verify: AudioCaptureEngine.targetFormat.channelCount == 1
           // Verify: AudioCaptureEngine.targetFormat.commonFormat == .pcmFormatFloat32
           throw XCTSkip("Awaiting AudioCaptureEngine implementation in plan 02-01")
       }

       func testPermissionCheckCalledBeforeStart() throws {
           // STUB: Plan 02-01 will implement
           // Verify: calling start() without permission throws AudioError.microphonePermissionDenied
           throw XCTSkip("Awaiting AudioCaptureEngine implementation in plan 02-01")
       }

       func testMaxFramesCapped() throws {
           // STUB: Plan 02-01 will implement
           // Verify: samples buffer does not exceed maxFrames (480_000)
           throw XCTSkip("Awaiting AudioCaptureEngine implementation in plan 02-01")
       }
   }
   ```

3. Create `Tests/TypenessTests/VADTests.swift`:
   ```swift
   import XCTest
   @testable import Typeness

   final class VADTests: XCTestCase {
       func testSilenceRejected() throws {
           // STUB: Plan 02-02 will implement after VADGate exists
           // Verify: VADGate.hasVoiceActivity(samples: [Float](repeating: 0.0, count: 16000)) == false
           throw XCTSkip("Awaiting VADGate implementation in plan 02-02")
       }

       func testShortRecordingRejected() throws {
           // STUB: Plan 02-02 will implement
           // Verify: VADGate.hasVoiceActivity(samples: [Float](repeating: 0.5, count: 4000)) == false
           throw XCTSkip("Awaiting VADGate implementation in plan 02-02")
       }

       func testLoudAudioAccepted() throws {
           // STUB: Plan 02-02 will implement
           // Verify: VADGate.hasVoiceActivity(samples: [Float](repeating: 0.5, count: 16000)) == true
           throw XCTSkip("Awaiting VADGate implementation in plan 02-02")
       }
   }
   ```

4. Create `Tests/TypenessTests/WhisperBridgeTests.swift`:
   ```swift
   import XCTest
   @testable import Typeness

   final class WhisperBridgeTests: XCTestCase {
       func testTranscribeThrowsWhenNotLoaded() async throws {
           // STUB: Plan 02-02 will implement after TranscriptionEngine exists
           // Verify: TranscriptionEngine().transcribe(audioFrames: []) throws TranscriptionError.notLoaded
           throw XCTSkip("Awaiting TranscriptionEngine implementation in plan 02-02")
       }

       func testTranscribeEmptyAudioThrows() async throws {
           // STUB: Plan 02-02 will implement
           // Verify: loaded engine with empty frames throws TranscriptionError.emptyAudio
           throw XCTSkip("Awaiting TranscriptionEngine implementation in plan 02-02")
       }
   }
   ```

5. Create the `Tests/TypenessTests/` directory if it does not exist.
  </action>
  <verify>
    <automated>cd /Users/yihao.wang/project/typeness-swift && swift build 2>&1 | tail -5 && swift test --list-tests 2>&1 | head -20</automated>
  </verify>
  <done>TypenessTests target compiles, `swift test --list-tests` shows AudioCaptureTests, VADTests, WhisperBridgeTests with stub methods.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds
- `swift test --list-tests` shows all three test classes
- All stubs skip cleanly (no failures, only skips)
</verification>

<success_criteria>
TypenessTests target exists in Package.swift, three stub test files compile and are discoverable by `swift test`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-capture-and-transcription/02-00-SUMMARY.md`
</output>
