---
phase: 02-audio-capture-and-transcription
plan: "02"
type: execute
wave: 1
depends_on: ["02-00"]
files_modified:
  - Typeness/Core/TranscriptionEngine.swift
  - Typeness/Core/VAD.swift
  - Package.swift
  - Tests/TypenessTests/VADTests.swift
  - Tests/TypenessTests/WhisperBridgeTests.swift
autonomous: true
requirements: [STT-01, STT-03]

must_haves:
  truths:
    - "Speaking audio frames produce a non-empty transcription string via whisper.cpp Metal backend"
    - "VAD gate rejects silence and short recordings before transcription"
  artifacts:
    - path: "Typeness/Core/TranscriptionEngine.swift"
      provides: "SwiftWhisper wrapper actor with load() and transcribe() methods"
      exports: ["TranscriptionEngine", "TranscriptionError"]
    - path: "Typeness/Core/VAD.swift"
      provides: "Energy-based VAD gate using vDSP"
      exports: ["VADGate"]
  key_links:
    - from: "Typeness/Core/TranscriptionEngine.swift"
      to: "SwiftWhisper"
      via: "Whisper(fromFileURL:) and transcribe(audioFrames:)"
      pattern: "import SwiftWhisper"
    - from: "Typeness/Core/VAD.swift"
      to: "Accelerate"
      via: "vDSP_rmsqv for RMS energy computation"
      pattern: "vDSP_rmsqv"
---

<objective>
Add SwiftWhisper SPM dependency, create TranscriptionEngine actor wrapping whisper.cpp, and create VAD energy gate to reject silence before transcription.

Purpose: Provides the speech-to-text inference pipeline and silence rejection that prevent hallucinated output.
Output: TranscriptionEngine.swift, VAD.swift, SwiftWhisper in Package.swift.
</objective>

<execution_context>
@/Users/yihao.wang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yihao.wang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-capture-and-transcription/02-RESEARCH.md

<interfaces>
From Typeness/Core/ModelManager.swift:
```swift
actor ModelManager {
    func whisperModelPath() -> URL  // ~/Library/Application Support/Typeness/Models/ggml-large-v3-turbo.bin
    func isWhisperModelDownloaded() -> Bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SwiftWhisper SPM dependency and create TranscriptionEngine actor</name>
  <files>Package.swift, Typeness/Core/TranscriptionEngine.swift, Tests/TypenessTests/WhisperBridgeTests.swift</files>
  <read_first>
    - Package.swift
    - Typeness/Core/ModelManager.swift
    - Tests/TypenessTests/WhisperBridgeTests.swift
    - .planning/phases/02-audio-capture-and-transcription/02-RESEARCH.md (Pattern 3, Open Questions 1 and 2)
  </read_first>
  <action>
1. Update Package.swift:
   - Add dependency: `.package(url: "https://github.com/exPHAT/SwiftWhisper.git", branch: "master")`
   - Add to target dependencies: `.product(name: "SwiftWhisper", package: "SwiftWhisper")`
   - Add `"Core/TranscriptionEngine.swift"` to sources array
   - Also add SwiftWhisper to the TypenessTests target dependencies if needed for test compilation

2. Create Typeness/Core/TranscriptionEngine.swift:
   - `import SwiftWhisper`
   - `enum TranscriptionError: Error { case notLoaded, emptyAudio }`
   - `actor TranscriptionEngine`:
     - `private var whisper: Whisper?`
     - `func load(modelURL: URL)` — creates `Whisper(fromFileURL: modelURL)`. After creation, attempt to set language to "zh": try `whisper?.params.language = WhisperLanguage(rawValue: "zh")`. If SwiftWhisper API differs, fall back to not setting language (whisper auto-detects Chinese accurately for large-v3-turbo). Add a comment explaining this.
     - `var isLoaded: Bool { whisper != nil }`
     - `func transcribe(audioFrames: [Float]) async throws -> String` — guard whisper is not nil (throw .notLoaded), guard audioFrames is not empty (throw .emptyAudio), call `try await whisper!.transcribe(audioFrames: audioFrames)`, join segments with `segments.map(\.text).joined().trimmingCharacters(in: .whitespacesAndNewlines)`, return result
     - If Whisper class is not Sendable-compatible inside the actor, use `nonisolated(unsafe)` on the whisper property per research recommendation

3. Update `Tests/TypenessTests/WhisperBridgeTests.swift` — replace `XCTSkip` stubs with real assertions:
   - `testTranscribeThrowsWhenNotLoaded`: Instantiate `TranscriptionEngine()`, call `try await engine.transcribe(audioFrames: [1.0])`, assert it throws `TranscriptionError.notLoaded`.
   - `testTranscribeEmptyAudioThrows`: This requires a loaded engine (needs model file). Keep as `XCTSkip` if model is not available in test environment, or test the guard logic if possible.
  </action>
  <verify>
    <automated>cd /Users/yihao.wang/project/typeness-swift && swift build 2>&1 | tail -10 && xcodebuild test -scheme Typeness -destination 'platform=macOS' -only-testing TypenessTests/WhisperBridgeTests 2>&1 | tail -20</automated>
  </verify>
  <done>SwiftWhisper compiles via SPM, TranscriptionEngine actor loads model and transcribes [Float] to String. WhisperBridgeTests pass (notLoaded assertion green).</done>
</task>

<task type="auto">
  <name>Task 2: Create VAD energy gate with vDSP</name>
  <files>Typeness/Core/VAD.swift, Package.swift, Tests/TypenessTests/VADTests.swift</files>
  <read_first>
    - Package.swift
    - Tests/TypenessTests/VADTests.swift
    - .planning/phases/02-audio-capture-and-transcription/02-RESEARCH.md (Pattern 4)
  </read_first>
  <action>
1. Create Typeness/Core/VAD.swift:
   - `import Accelerate`
   - `enum VADGate`:
     - `static let defaultThreshold: Float = 0.01` (approximately -40 dBFS)
     - `static let minimumDurationSeconds: Double = 0.5`
     - `static let sampleRate: Double = 16_000`
     - `static func hasVoiceActivity(samples: [Float], threshold: Float = defaultThreshold) -> Bool` — first check `samples.count >= Int(sampleRate * minimumDurationSeconds)` (return false if too short, i.e. less than 8000 frames), then compute RMS via `var rms: Float = 0; vDSP_rmsqv(samples, 1, &rms, vDSP_Length(samples.count))`, return `rms > threshold`

2. Add `"Core/VAD.swift"` to Package.swift sources array.

3. Update `Tests/TypenessTests/VADTests.swift` — replace `XCTSkip` stubs with real assertions:
   - `testSilenceRejected`: `XCTAssertFalse(VADGate.hasVoiceActivity(samples: [Float](repeating: 0.0, count: 16000)))`
   - `testShortRecordingRejected`: `XCTAssertFalse(VADGate.hasVoiceActivity(samples: [Float](repeating: 0.5, count: 4000)))`
   - `testLoudAudioAccepted`: `XCTAssertTrue(VADGate.hasVoiceActivity(samples: [Float](repeating: 0.5, count: 16000)))`
  </action>
  <verify>
    <automated>cd /Users/yihao.wang/project/typeness-swift && swift build 2>&1 | tail -5 && xcodebuild test -scheme Typeness -destination 'platform=macOS' -only-testing TypenessTests/VADTests 2>&1 | tail -20</automated>
  </verify>
  <done>VADGate rejects silence (RMS below 0.01) and short recordings (under 0.5s) before transcription. All three VADTests pass green.</done>
</task>

</tasks>

<verification>
- `swift build` succeeds with SwiftWhisper dependency resolved
- TranscriptionEngine.swift and VAD.swift exist and compile
- `xcodebuild test -only-testing TypenessTests/VADTests` all green
- `xcodebuild test -only-testing TypenessTests/WhisperBridgeTests` passes (notLoaded test green)
</verification>

<success_criteria>
SwiftWhisper builds via SPM, TranscriptionEngine can load a model and transcribe audio frames, VADGate filters silence and short recordings. Unit tests verify VAD logic and transcription error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-capture-and-transcription/02-02-SUMMARY.md`
</output>
