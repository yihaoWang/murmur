---
phase: 03-llm-post-processing-and-text-insertion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - Package.swift
  - Tests/TypenessTests/LLMProcessorTests.swift
  - Tests/TypenessTests/TextInserterTests.swift
autonomous: true
requirements: [LLM-01, LLM-02, INSERT-01, INSERT-02, INSERT-03, INSERT-04]

must_haves:
  truths:
    - "mlx-swift-lm SPM dependency resolves and builds"
    - "Test stubs exist for all Phase 3 requirements"
  artifacts:
    - path: "Tests/TypenessTests/LLMProcessorTests.swift"
      provides: "Test stubs for LLM-01, LLM-02"
    - path: "Tests/TypenessTests/TextInserterTests.swift"
      provides: "Test stubs for INSERT-01 through INSERT-04"
  key_links:
    - from: "Package.swift"
      to: "mlx-swift-lm"
      via: "SPM dependency declaration"
      pattern: "mlx-swift-lm"
---

<objective>
Add mlx-swift-lm SPM dependency and create Wave 0 test stubs for all Phase 3 requirements.

Purpose: Establish the test scaffolding before implementation (Nyquist compliance) and verify mlx-swift-lm builds.
Output: Package.swift with LLM dependency, two test files with XCTSkip stubs.
</objective>

<execution_context>
@/Users/yihao.wang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yihao.wang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-post-processing-and-text-insertion/03-RESEARCH.md
@.planning/phases/03-llm-post-processing-and-text-insertion/03-VALIDATION.md

<interfaces>
From Typeness/Core/AppState.swift:
```swift
@Observable final class AppState {
    var modelDownloadProgress: Double? = nil
    var isWhisperModelReady: Bool = false
    var recordingState: RecordingState = .idle
    var lastTranscription: String = ""
}
```

From Package.swift — existing dependencies:
```swift
.package(url: "https://github.com/sindresorhus/KeyboardShortcuts", .upToNextMinor(from: "1.15.0")),
.package(url: "https://github.com/exPHAT/SwiftWhisper.git", branch: "master")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mlx-swift-lm SPM dependency and update Package.swift</name>
  <files>Package.swift</files>
  <read_first>
    - Package.swift
    - .planning/phases/03-llm-post-processing-and-text-insertion/03-RESEARCH.md
  </read_first>
  <action>
1. Add mlx-swift-lm to Package.swift dependencies array:
   ```swift
   .package(url: "https://github.com/ml-explore/mlx-swift-lm/", exact: "2.30.6")
   ```
   Use `.exact("2.30.6")` for reproducibility per research recommendation.

2. Add LLM and MLXLMCommon product dependencies to the "Typeness" executableTarget:
   ```swift
   .product(name: "LLM", package: "mlx-swift-lm"),
   .product(name: "MLXLMCommon", package: "mlx-swift-lm"),
   ```

3. Add new source files to the sources array (they will be created in later plans):
   ```swift
   "Core/PostProcessingEngine.swift",
   "Core/TextInsertionEngine.swift",
   ```

4. Add LLM dependency to the testTarget as well so tests can import LLM types:
   ```swift
   .product(name: "LLM", package: "mlx-swift-lm"),
   .product(name: "MLXLMCommon", package: "mlx-swift-lm"),
   ```

5. Run `swift package resolve` to verify dependency resolution succeeds.

Note: Do NOT run `swift build` yet — source files referenced don't exist yet. Just verify resolution.
  </action>
  <verify>
    <automated>cd /Users/yihao.wang/project/typeness-swift && swift package resolve 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Package.swift contains `"mlx-swift-lm"` in dependencies
    - Package.swift contains `.product(name: "LLM", package: "mlx-swift-lm")` in Typeness target
    - Package.swift contains `"Core/PostProcessingEngine.swift"` in sources array
    - Package.swift contains `"Core/TextInsertionEngine.swift"` in sources array
    - `swift package resolve` exits 0
  </acceptance_criteria>
  <done>mlx-swift-lm dependency resolves successfully; Package.swift updated with new source file entries</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for Phase 3 requirements</name>
  <files>Tests/TypenessTests/LLMProcessorTests.swift, Tests/TypenessTests/TextInserterTests.swift</files>
  <read_first>
    - .planning/phases/03-llm-post-processing-and-text-insertion/03-VALIDATION.md
    - Tests/TypenessTests/AudioCaptureTests.swift
  </read_first>
  <action>
1. Create `Tests/TypenessTests/LLMProcessorTests.swift` following the same XCTSkip pattern used in Phase 2 stubs (see AudioCaptureTests.swift for style). Include:
   - `testFormatThrowsWhenNotLoaded` — XCTSkip("PostProcessingEngine not yet implemented") [LLM-01]
   - `testNoSpacesBetweenChineseChars` — XCTSkip("PostProcessingEngine not yet implemented") [LLM-02]
   - `testModelLoadProgressReported` — XCTSkip("Requires network and 968 MB download") [LLM-03]

2. Create `Tests/TypenessTests/TextInserterTests.swift` with:
   - `testAccessibilityInsertReturnsPath` — XCTSkip("Requires live AX focused element") [INSERT-01]
   - `testClipboardFallbackReturnsPath` — XCTSkip("Requires AX failure condition") [INSERT-02]
   - `testClipboardRestored` — XCTSkip("TextInsertionEngine not yet implemented") [INSERT-03]
   - `testTransientTypeMarkerPresent` — XCTSkip("TextInsertionEngine not yet implemented") [INSERT-04]

3. Each file should `import XCTest` and `@testable import Typeness`. Use `final class LLMProcessorTests: XCTestCase` and `final class TextInserterTests: XCTestCase`.

4. Verify tests compile and all skip by running xcodebuild test. Since Package.swift source files don't exist yet, create minimal placeholder files first:
   - Create `Typeness/Core/PostProcessingEngine.swift` with just: `import Foundation` and `enum PostProcessingError: Error { case notLoaded }`
   - Create `Typeness/Core/TextInsertionEngine.swift` with just: `import Foundation`
   This ensures the build succeeds so tests can run.
  </action>
  <verify>
    <automated>cd /Users/yihao.wang/project/typeness-swift && xcodebuild test -scheme Typeness -destination 'platform=macOS' -only-testing TypenessTests/LLMProcessorTests -only-testing TypenessTests/TextInserterTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `Tests/TypenessTests/LLMProcessorTests.swift` exists and contains `class LLMProcessorTests: XCTestCase`
    - File `Tests/TypenessTests/TextInserterTests.swift` exists and contains `class TextInserterTests: XCTestCase`
    - LLMProcessorTests contains `testFormatThrowsWhenNotLoaded`, `testNoSpacesBetweenChineseChars`, `testModelLoadProgressReported`
    - TextInserterTests contains `testAccessibilityInsertReturnsPath`, `testClipboardFallbackReturnsPath`, `testClipboardRestored`, `testTransientTypeMarkerPresent`
    - All tests skip (not fail) when run
    - `Typeness/Core/PostProcessingEngine.swift` exists as minimal placeholder
    - `Typeness/Core/TextInsertionEngine.swift` exists as minimal placeholder
  </acceptance_criteria>
  <done>7 test stubs exist covering all Phase 3 requirement IDs; all skip cleanly; placeholder source files allow build</done>
</task>

</tasks>

<verification>
- `swift package resolve` succeeds (dependency resolution)
- `xcodebuild test -scheme Typeness -destination 'platform=macOS' -only-testing TypenessTests/LLMProcessorTests -only-testing TypenessTests/TextInserterTests` shows all tests skipped
</verification>

<success_criteria>
- mlx-swift-lm 2.30.6 resolves as SPM dependency
- 7 test stubs exist covering LLM-01, LLM-02, LLM-03, INSERT-01, INSERT-02, INSERT-03, INSERT-04
- All tests skip (not fail)
- Placeholder source files allow project to build
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-post-processing-and-text-insertion/03-00-SUMMARY.md`
</output>
